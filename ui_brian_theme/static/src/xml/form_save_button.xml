<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="ui_brian_theme.FormView" t-inherit="web.FormView" t-inherit-mode="extension">
        <xpath expr="//t[@t-set-slot='control-panel-status-indicator']" position="replace">
            <t t-set-slot="control-panel-status-indicator">
                <div t-if="canEdit or uiBrianViewTitle" class="o_ui_brian_form_actions d-flex align-items-center me-auto">
                    <t t-if="canEdit and uiBrianShowSaveDiscardButtons">
                        <button
                            type="button"
                            class="btn btn-primary"
                            data-hotkey="s"
                            t-att-disabled="!model.root.isNew and !model.root.isInEdition ? true : undefined"
                            t-on-click.stop="() => this.uiBrianManualSave()">
                            Guardar
                        </button>
                        <button
                            t-if="model.root.isNew or model.root.isInEdition"
                            type="button"
                            class="btn btn-secondary"
                            data-hotkey="j"
                            t-on-click.stop="discard">
                            Descartar
                        </button>
                    </t>
                    <span
                        t-if="!model.root.isNew and !model.root.isValid"
                        class="text-danger small">
                        <i class="fa fa-warning me-1"/>
                        Revisa los campos antes de guardar
                    </span>
                    <span
                        t-if="uiBrianViewTitle"
                        class="o_ui_brian_view_title o_ui_brian_view_title--form"
                        t-esc="uiBrianViewTitle"/>
                </div>
            </t>
        </xpath>
    </t>

    <t t-name="ui_brian_theme.FormViewButtons" t-inherit="web.FormView.Buttons" t-inherit-mode="extension">
        <xpath expr="//div[contains(@class, 'o_form_buttons_edit')]" position="attributes">
            <attribute name="t-if">model.root.isInEdition and uiBrianShowSaveDiscardButtons</attribute>
        </xpath>
        <xpath expr="//div[contains(@class, 'o_form_buttons_view')]" position="attributes">
            <attribute name="t-elif">canCreate and uiBrianShowSaveDiscardButtons</attribute>
        </xpath>
        <xpath expr="//button[contains(@class, 'o_form_button_save')]" position="attributes">
            <attribute name="t-on-click.stop">() => this.uiBrianManualSave({ closable: true })</attribute>
        </xpath>
    </t>

    <t t-name="ui_brian_theme.FormViewDialog" t-inherit="web.FormViewDialog" t-inherit-mode="extension">
        <xpath expr="//t[@t-set-slot='footer']" position="replace">
            <t t-set-slot="footer">
                <t t-if="!uiBrianHideDialogFooter">
                    <t t-if="props.preventEdit and props.preventCreate">
                        <button class="btn btn-primary" t-on-click="() => props.close()">Close</button>
                    </t>
                    <t t-else="">
                        <button class="btn btn-primary o-default-button" t-on-click="() => props.close()">Ok</button>
                    </t>
                </t>
            </t>
        </xpath>
    </t>

    <t t-name="ui_brian_theme.FormViewDialogToManyButtons" t-inherit="web.FormViewDialog.ToMany.buttons" t-inherit-mode="extension">
        <xpath expr="//div[contains(@class, 'o_form_buttons_edit')]" position="attributes">
            <attribute name="t-if">model.root.isInEdition and uiBrianShowSaveDiscardButtons</attribute>
        </xpath>
        <xpath expr="//div[contains(@class, 'o_form_buttons_view')]" position="attributes">
            <attribute name="t-elif">canCreate and uiBrianShowSaveDiscardButtons</attribute>
        </xpath>
    </t>

    <t t-name="ui_brian_theme.FormViewDialogToOneButtons" t-inherit="web.FormViewDialog.ToOne.buttons" t-inherit-mode="extension">
        <xpath expr="//div[contains(@class, 'o_form_buttons_edit')]" position="attributes">
            <attribute name="t-if">model.root.isInEdition and uiBrianShowSaveDiscardButtons</attribute>
        </xpath>
        <xpath expr="//div[contains(@class, 'o_form_buttons_view')]" position="attributes">
            <attribute name="t-elif">canCreate and uiBrianShowSaveDiscardButtons</attribute>
        </xpath>
    </t>

</templates>
